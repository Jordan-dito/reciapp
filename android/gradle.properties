org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# --- Java 21 notes ---------------------------------------------------------
# To build with Java 21, install a JDK 21 distribution and set Gradle to use it.
# On Windows you can set the absolute path to your JDK 21 installation here,
# for example:
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.0
# Remove the leading '#' and adjust the path after you install JDK 21.
# Alternatively, set JAVA_HOME system variable or configure the IDE to point
# to the JDK 21 installation.
# ---------------------------------------------------------------------------
# Force Gradle to use JDK 17 for Android builds (installed via winget)
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.17.10-hotspot
